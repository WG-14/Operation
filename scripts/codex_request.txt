You are working in the GitHub repository `WG-14/bithumb-bot`.

The latest patch appears to have closed most of the approved-profile contract gaps: ambiguous live flag reason codes are now passed through CLI/runtime verification, selector alias precedence is more consistent, `load_profile_or_promotion_regime_policy()` has a `verify_source` option, and `profile-diff` now clarifies that it does not verify the full artifact chain.

Do **not** assume the implementation is complete. Your task is to directly inspect the repo, verify the remaining hypotheses below, and apply only the additional patches needed to preserve accurate runtime auditability and artifact-chain integrity.

The main risk now is not automatic trading safety being bypassed. The main risk is **observability drift**: a normal approved-profile runtime path may still be reported as legacy/regime-policy-only, which undermines the goal of reproducible, auditable operation.

## Core safety and audit objective

Preserve this model:

```text
research evidence
  ↓
promotion artifact
  ↓ operator review
approved paper profile
  ↓ paper validation evidence
approved live-dry-run profile
  ↓ live readiness evidence
approved small-live profile
```

The success criterion is:

> A runtime using a full approved profile must be auditable as a full approved profile. A legacy candidate-profile compatibility path must be auditable as legacy/regime-policy-only. These two paths must never be conflated.

Do **not**:

* connect the research registry directly to live execution
* auto-copy research parameters into env files
* auto-arm live trading
* weaken live arming checks
* weaken DB/storage-root isolation
* weaken lot-native SELL authority
* weaken ledger/reconciliation protections
* require exchange credentials for profile CLI/tests
* make deterministic hashes depend on timestamps
* hide legacy compatibility usage behind fields that look like full verification

## Hypotheses to verify or falsify

Treat each item as a hypothesis, not a fact. Inspect the repo before modifying code.

### Hypothesis A — `strategy_config.py` may mark approved-profile usage as legacy/regime-policy-only

In `strategy_config.py`, the code may distinguish approved selector vs legacy selector when loading the profile, but then unconditionally append fields like:

```python
policy = {
    **policy,
    "legacy_candidate_profile_path_used": True,
    "legacy_profile_contract_scope": "regime_policy_only",
    "legacy_profile_selector_env": LEGACY_PROFILE_SELECTOR_ENV,
}
```

If this is true, then even a normal approved-profile path where `raw_path == approved_profile_path` may be reported in runtime audit/decision context as legacy/regime-policy-only.

This is a high-priority observability bug. It can make a correctly verified approved profile appear as a legacy path, corrupting postmortem and reproducibility evidence.

Required patch if verified:

```python
if policy is not None:
    if raw_path == approved_profile_path:
        policy = {
            **policy,
            "legacy_candidate_profile_path_used": False,
            "legacy_profile_contract_scope": "full_approved_profile",
            "approved_profile_contract_scope": "full_approved_profile",
        }
    else:
        policy = {
            **policy,
            "legacy_candidate_profile_path_used": True,
            "legacy_profile_contract_scope": "regime_policy_only",
            "approved_profile_contract_scope": "legacy_regime_policy_only",
            "legacy_profile_selector_env": LEGACY_PROFILE_SELECTOR_ENV,
        }
```

Adapt field names to existing repo conventions, but preserve the distinction clearly.

Required tests:

* When `APPROVED_STRATEGY_PROFILE_PATH` selects an approved profile:

  * `legacy_candidate_profile_path_used == False`
  * `legacy_profile_contract_scope == "full_approved_profile"` or the legacy field is absent
  * `approved_profile_contract_scope == "full_approved_profile"`
  * `approved_profile_verification_ok == True`
  * approved profile hash/source/promotion fields remain present
* When only `STRATEGY_CANDIDATE_PROFILE_PATH` is used in paper/replay compatibility mode:

  * `legacy_candidate_profile_path_used == True`
  * `legacy_profile_contract_scope == "regime_policy_only"`
  * `approved_profile_contract_scope == "legacy_regime_policy_only"` or equivalent
* In live mode with only legacy path and no approved profile selector:

  * live still fails closed before BUY
  * failure remains `approved_profile_missing` or the repo’s existing equivalent reason
* No approved-profile success path should be labeled legacy/regime-policy-only.

Priority: **high**.

---

### Hypothesis B — `approved_profile_verification_ok` may be overloaded between full verification and legacy regime-only loading

`load_profile_or_promotion_regime_policy()` may return:

```python
source_verified = bool(verify_source)
block_reason = "ok" if source_verified else "legacy_regime_policy_only_source_not_verified"
"approved_profile_verification_ok": source_verified
```

This is conservative, but the meaning can become ambiguous if later code also attaches legacy markers incorrectly.

Verify whether the repo cleanly separates:

* profile loaded
* profile schema hash validated
* source promotion verified
* evidence verified
* runtime/env contract verified
* full approved-profile contract active
* legacy regime-policy-only compatibility active

Required patch if verified:

Introduce or standardize explicit audit fields, for example:

```json
{
  "approved_profile_loaded": true,
  "approved_profile_schema_hash_valid": true,
  "approved_profile_source_verified": true,
  "approved_profile_evidence_verified": true,
  "approved_profile_runtime_verified": true,
  "approved_profile_contract_scope": "full_approved_profile",
  "legacy_candidate_profile_path_used": false
}
```

For legacy/regime-only compatibility paths:

```json
{
  "approved_profile_loaded": true,
  "approved_profile_schema_hash_valid": true,
  "approved_profile_source_verified": false,
  "approved_profile_evidence_verified": false,
  "approved_profile_runtime_verified": false,
  "approved_profile_contract_scope": "legacy_regime_policy_only",
  "legacy_candidate_profile_path_used": true
}
```

Do not make legacy regime-only paths look like full approved-profile verification.

Required tests:

* Full approved profile path emits clear full-verification fields.
* Legacy regime-only path emits clear partial/legacy fields.
* Source/evidence drift on a full approved profile does not produce ambiguous “loaded but ok” telemetry.
* Runtime mismatch on a full approved profile produces a reason-coded failure.

Priority: **medium-high**.

---

### Hypothesis C — `profile-promote` may still not re-verify parent source/evidence drift before creating a child profile

The CLI may still do:

```python
parent = load_approved_profile(profile_path)
child = promote_profile_mode(...)
```

This is safe only if `promote_profile_mode()` itself calls `verify_profile_source_promotion(parent_profile)` before creating the child profile.

Verify the full implementation of `promote_profile_mode()`.

Required patch if verified:

At the start of `promote_profile_mode()`:

```python
verify_profile_source_promotion(parent_profile)
```

This must check both:

* source promotion artifact drift
* parent evidence artifact drift

Do not rely only on `validate_approved_profile(parent_profile)`.

Required tests:

* Generate paper profile.
* Mutate source promotion artifact.
* Attempt `profile-promote --mode live_dry_run`.
* It must fail with `source_promotion_content_hash_mismatch` or equivalent.
* Generate live-dry-run profile with paper validation evidence.
* Mutate paper validation evidence.
* Attempt `profile-promote --mode small_live`.
* It must fail with `paper_validation_evidence_content_hash_mismatch` or equivalent.
* Failure must be visible in CLI JSON.

Priority: **high**, unless already implemented and tested.

---

### Hypothesis D — artifact custody path policy may still be only “outside repo,” not “inside data root/reports”

`resolve_runtime_artifact_path()` may reject repo-local paths and require file existence, but still allow arbitrary external absolute paths such as `/tmp/random.json`.

This is not necessarily wrong, but the policy must be explicit. For a live trading system, artifact custody is part of reproducibility.

Verify current behavior and docs for:

* source promotion artifact paths
* paper validation evidence paths
* live readiness evidence paths
* approved profile output paths

Required action:

Choose one policy and implement/document it consistently.

Option 1 — strict custody:

* live-dry-run and small-live profile promotion must require source/evidence artifacts under `PathManager.data_dir()` or reports/profile roots.
* repo-local paths remain rejected.
* arbitrary external paths are rejected for live transitions.
* paper profile generation may be looser only if explicitly documented.

Option 2 — external absolute paths allowed:

* repo-local paths remain rejected.
* external absolute paths are accepted.
* all external artifacts are content-hashed.
* docs clearly state that operators are responsible for custody of external evidence/source artifacts.
* CLI output should expose absolute paths and hashes clearly.

Required tests depending on chosen policy:

* repo-local artifact/evidence path rejected
* missing path rejected
* data-root/reports path accepted
* external absolute path accepted or rejected according to documented policy
* live promotion follows the stricter policy if implemented

Priority: **medium**.

---

## Required implementation priorities

Implement in this order unless repo inspection reveals a more severe safety issue:

1. Fix approved-profile audit labeling so approved paths are never marked legacy/regime-policy-only.
2. Clarify verification audit semantics so full approved-profile verification and legacy regime-only loading are visibly distinct.
3. Verify `promote_profile_mode()` re-checks parent source/evidence drift; patch if missing.
4. Decide and document artifact custody path policy.

## Runtime safety requirements

Live mode:

* Missing approved profile must fail closed before BUY.
* Legacy candidate profile path must not satisfy approved live profile requirements.
* Approved profile success path must not be labeled legacy.
* Profile/env mismatch must fail closed before BUY.
* Source promotion drift must fail closed before BUY.
* Evidence drift must fail closed before BUY if evidence is part of the active profile chain.
* Ambiguous live flags must fail with precise reason codes.
* Successful verification must leave audit fields sufficient to reproduce the active artifact chain.

Paper/replay mode:

* Approved profile selector path should be audited as full approved-profile if source/runtime verification succeeds.
* Legacy candidate path may remain as compatibility/regime-policy-only, but it must be visibly marked as such.
* Legacy compatibility must not be confused with full approved-profile verification.

SELL/exit:

* Do not block SELL/exit solely because candidate regime policy or approved profile verification failed, unless existing repo safety rules explicitly require that behavior.
* Add or preserve regression tests for this.

## Observability requirements

For a full approved-profile path, runtime/preflight/decision context should expose:

* `approved_profile_path`
* `approved_profile_mode`
* `approved_profile_hash`
* `approved_profile_verification_ok`
* `approved_profile_block_reason`
* `approved_profile_contract_scope = full_approved_profile`
* `approved_profile_source_verified`
* `approved_profile_evidence_verified`
* `approved_profile_runtime_verified`
* `legacy_candidate_profile_path_used = false`
* `source_promotion_artifact_path`
* `promotion_content_hash`
* `candidate_profile_hash`
* `manifest_hash`
* `dataset_content_hash`
* evidence paths and evidence hashes when present
* mismatch count/list on failures

For a legacy/regime-policy-only path, runtime/preflight/decision context should expose:

* `legacy_candidate_profile_path_used = true`
* `legacy_profile_contract_scope = regime_policy_only`
* `approved_profile_contract_scope = legacy_regime_policy_only`
* source/runtime/evidence verification fields should be false or absent, not misleadingly true
* block reason should make clear that this is not a full approved-profile contract

Do not log secrets.

## Recovery / rollback requirements

Ensure or document:

* failed verification stops live BUY before order submission
* operator can roll back by changing only the approved profile selector to a previous approved profile
* profile generation and promotion writes are atomic
* CLI errors are deterministic and machine-readable where JSON output exists
* no CLI mutates live env files automatically
* audit fields make rollback verification reproducible

## Documentation updates

Update README, research validation docs, runbooks, and env examples as needed.

Docs must clearly state:

* approved selector path is the full approved-profile contract path
* legacy `STRATEGY_CANDIDATE_PROFILE_PATH` is compatibility/regime-policy-only if still supported
* approved profile success paths are not legacy
* `approved_profile_verification_ok` means full verification only
* what fields indicate source/evidence/runtime verification
* whether external absolute artifact paths are allowed
* `profile-promote` re-verifies parent source/evidence drift before child creation
* live fails closed on missing/mismatched/drifted approved profile
* SELL/exit policy remains unchanged

## Required tests

Add or update tests in the repo’s existing style.

At minimum:

### Approved vs legacy audit labeling

* Approved selector path returns `legacy_candidate_profile_path_used == False`.
* Approved selector path returns full approved-profile contract scope.
* Approved selector path returns `approved_profile_verification_ok == True`.
* Legacy candidate path returns `legacy_candidate_profile_path_used == True`.
* Legacy candidate path returns regime-policy-only contract scope.
* Live mode with only legacy candidate path still fails approved profile requirement.

### Verification semantic clarity

* Full approved-profile path distinguishes:

  * loaded
  * source verified
  * evidence verified
  * runtime verified
  * full contract scope
* Legacy regime-only path does not claim source/runtime/evidence verification.
* Runtime mismatch produces reason-coded approved-profile failure.
* Source/evidence drift produces reason-coded approved-profile failure.

### Promotion chain drift

* `profile-promote` fails if source promotion artifact changed after parent profile creation.
* `profile-promote` fails if parent evidence artifact changed after parent profile creation.
* CLI JSON exposes the specific failure reason.

### Artifact custody policy

Depending on chosen policy:

* repo-local path rejected
* missing path rejected
* data-root/reports path accepted
* external absolute path accepted or rejected exactly as documented
* live promotion policy is tested if stricter than paper policy

### Existing safety regression

Run or preserve tests for:

* profile-generate
* profile-diff
* profile-verify
* profile-promote
* live preflight
* arming
* run lock
* reconciliation
* duplicate-order prevention
* ledger behavior
* storage-root isolation
* lot-native SELL authority
* SELL/exit not blocked solely by candidate regime/profile verification failure

## Final report format

When finished, produce this exact report:

```markdown
## Summary

## Hypotheses Verified / Falsified

For each hypothesis A–D:
- status: verified, partially verified, falsified, or inconclusive
- repo evidence
- implemented change or reason no change was needed

## Design Changes

## Runtime Safety Behavior

Explain behavior for:
- paper
- replay
- live dry-run
- live armed / small-live
- SELL/exit paths

## Approved vs Legacy Audit Semantics

Explain exactly how a full approved-profile path differs from legacy/regime-policy-only compatibility in emitted fields.

## Artifact Chain Verification

Explain:
- parent profile verification
- source promotion verification
- evidence verification
- runtime/env verification
- path custody policy

## CLI Behavior

Include expected behavior for:
- profile-generate
- profile-diff
- profile-verify
- profile-promote

## Observability / Audit Fields

List fields emitted on success and failure.

## Recovery / Rollback

Explain operator recovery from failed verification and rollback to a previous approved profile.

## Tests Added or Updated

List test files and what each verifies.

## Backward Compatibility

Explain behavior for:
- old promotion artifacts
- old approved profiles
- legacy `STRATEGY_CANDIDATE_PROFILE_PATH`
- selector aliases

## Validation

Include exact commands run and results.

## Remaining Risks / Follow-ups

Only list intentionally deferred work or unresolved hypotheses.
```

Success criterion:

> A normal approved-profile runtime path is auditable as a full approved-profile contract, a legacy candidate-profile path is auditable as legacy/regime-policy-only, live execution still fails closed on dangerous drift, and operators can reproduce which artifact chain was actually active.

