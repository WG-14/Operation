You are Codex working directly in the GitHub repository `WG-14/bithumb-bot`.

Use the attached review note as the source context for this follow-up patch. Do not weaken the core concern or turn this into generic validation work. The previous patch moved the repository in the right direction by adding `DatasetQualityReport`, wiring dataset quality into research backtest/walk-forward candidate gates, surfacing dataset quality hash/status in promotion/profile evidence, and adding pytest coverage. However, the research evidence chain is still not fully closed. The next patch must address the remaining structural gaps, especially execution calibration assumptions and scalable dataset quality computation. 

Core concern:

The repository is evolving from a candle-only SMA research tool into a reproducible, evidence-bound automatic trading experiment system. The current remaining risk is that research candidates can still appear valid even when real execution quality is materially worse than the research scenario assumptions, especially for partial fills, unfilled orders, insufficient execution sample size, or failed execution-quality gates. A second remaining risk is that dataset quality reporting may work for small tests but become unnecessarily memory-heavy or misleading on long historical 1m splits.

Do not treat this as a cosmetic patch. Treat it as closing the contract:

real execution quality → calibration artifact → research execution scenario → candidate gate → promotion evidence → operator-visible diagnostics.

Work on `main` unless instructed otherwise.

Inspect first:

* `src/bithumb_bot/research/execution_calibration.py`
* `src/bithumb_bot/research/validation_protocol.py`
* `src/bithumb_bot/research/dataset_snapshot.py`
* `src/bithumb_bot/research/promotion_gate.py`
* `src/bithumb_bot/research/lineage.py`
* `src/bithumb_bot/execution_quality.py`
* `src/bithumb_bot/research/experiment_manifest.py`
* `tests/test_dataset_quality_report.py`
* `tests/test_execution_quality.py`
* any existing research calibration, promotion, or reproducibility tests
* `docs/research-validation.md`

Keep these as hypotheses until verified in code.

Hypothesis 1:
`compare_calibration_to_scenario()` currently validates market, interval, content hash, p90/p95 slippage, p95 latency, and model breach rate, but does not compare calibration `partial_fill_rate` or `unfilled_rate` against the scenario’s `partial_fill_rate` and `order_failure_rate`.

Verification:
Inspect `src/bithumb_bot/research/execution_calibration.py` and all call sites. Confirm whether `partial_fill_rate`, `unfilled_rate`, `quality_gate_status`, and sample count thresholds are actually enforced. If they are not, implement the enforcement.

Required patch:
Extend `compare_calibration_to_scenario()` with these inputs, or equivalent names if a cleaner existing policy object exists:

```python
assumed_partial_fill_rate: float = 0.0
assumed_order_failure_rate: float = 0.0
min_sample_count: int | None = None
require_quality_gate_pass: bool = False
```

Add deterministic fail reasons:

```text
execution_calibration_partial_fill_rate_exceeds_assumption
execution_calibration_unfilled_rate_exceeds_assumption
execution_calibration_sample_count_below_required
execution_calibration_quality_gate_not_passed
```

Use `partial_fill_rate` from the calibration artifact as observed partial-fill rate. Use `unfilled_rate` from the calibration artifact as observed order-failure/unfilled rate. Compare these against the scenario assumptions. If the scenario assumes zero partial fills or zero order failures, observed positive partial/unfilled rates must fail when calibration is required or strictness requires failure.

Update `validation_protocol.py` so each scenario passes:

* `scenario.partial_fill_rate`
* `scenario.order_failure_rate`
* an appropriate minimum sample threshold if a repository policy already exists
* `require_quality_gate_pass=True` when calibration is required, or when the repository’s strictness policy says calibration must be clean

Do not silently pass if the calibration artifact contains a failed execution-quality gate and the manifest requires calibration.

Hypothesis 2:
The current dataset quality implementation builds all expected timestamps with `tuple(range(...))` and may also build an `expected_set`. This is acceptable in tiny unit tests but is not scalable for long 1m research splits.

Verification:
Inspect `build_dataset_quality_report()` in `src/bithumb_bot/research/dataset_snapshot.py`. Confirm whether it materializes the full expected timestamp range and expected set.

Required patch:
Refactor dataset quality expected-bucket handling to be streaming or bounded-memory.

Requirements:

* Compute `expected_candle_count` by arithmetic, not by materializing a full tuple.
* Scan expected bucket timestamps with a generator or loop.
* Keep only bounded missing samples and compact missing ranges.
* Do not build an unbounded `expected_set` for long ranges.
* Keep output deterministic.
* Preserve existing fields unless replacing them with a strictly better equivalent.
* Keep `missing_bucket_ranges` bounded.
* Keep `missing_bucket_sample` bounded.
* Preserve `content_hash` determinism.

Hypothesis 3:
Current `coverage_pct` may be computed as `actual_candle_count / expected_candle_count`, which can overstate coverage if duplicate, malformed, or unexpected buckets exist.

Verification:
Inspect the current coverage calculation.

Required patch:
Compute coverage from expected buckets actually present:

```text
coverage_pct = present_expected_bucket_count / expected_candle_count * 100
```

Add or preserve these fields:

```json
{
  "present_expected_bucket_count": 1438,
  "actual_candle_count": 1440,
  "unexpected_bucket_count": 2,
  "coverage_pct": 99.86111111
}
```

`actual_candle_count` should remain the raw number of loaded candle rows for the split. `present_expected_bucket_count` should count unique expected buckets present in the dataset. `coverage_pct` must not exceed 100.

Hypothesis 4:
Legacy/no-lineage promotion can bypass dataset quality verification in `_verify_report_dataset_quality()`. This may be acceptable only as an explicit compatibility path, but it should be auditable and not accidentally treated as full new-generation evidence.

Verification:
Inspect `promotion_gate.py`, especially `_verify_report_dataset_quality()` and `promote_candidate(... allow_legacy_lineage=...)`. Confirm whether missing lineage skips dataset quality checks and whether warnings are recorded.

Required patch:
If `allow_legacy_lineage=True` allows promotion without new dataset quality evidence, make the bypass explicit and visible.

Add promotion fields or equivalent:

```json
{
  "dataset_quality_legacy_bypass_used": true
}
```

Add promotion warning:

```text
legacy_dataset_quality_bypass_used
```

Do not let a legacy dataset-quality bypass masquerade as full verified evidence. If approved profile generation or live/small_live promotion already rejects legacy evidence, confirm and document. If it does not, patch it so full live readiness cannot rely on a hidden dataset-quality bypass.

Hypothesis 5:
Top-of-book storage and research join are still out of scope for this patch. They should not distract from P0/P1 fixes, but the final report should clearly mark them as the next PR if not implemented.

Verification:
Check whether `orderbook_top_snapshots`, `sync-orderbook-top`, quote coverage, and `ExecutionRequest.best_bid/best_ask/spread_bps` population in research now exist. If not, do not implement unless all P0/P1 work is complete and low-risk.

Scope decision:
This patch should prioritize:

1. P0 calibration gate closure
2. P0.5 scalable dataset quality computation
3. P1 coverage correctness
4. P1 explicit legacy dataset-quality bypass warning
5. P2 documentation and next-PR scoping for top-of-book

Do not start a large top-of-book implementation if it risks destabilizing the calibration and dataset-quality fixes.

Testing requirements:

Add or update tests. Do not rely only on `pytest` passing from the previous patch.

Minimum new/updated tests:

Execution calibration:

1. Required calibration fails when observed `partial_fill_rate` exceeds scenario `partial_fill_rate`.
2. Required calibration fails when observed `unfilled_rate` exceeds scenario `order_failure_rate`.
3. Required calibration fails when `sample_count` is below the required threshold, if a threshold is configured.
4. Required calibration fails when artifact `quality_gate_status=FAIL` and `require_quality_gate_pass=True`.
5. Calibration still passes when observed partial/unfilled rates are within scenario assumptions.
6. Calibration warning-mode behavior remains explicit and does not silently pass required calibration.
7. Candidate gate in `validation_protocol.py` receives the new calibration fail reasons and marks the scenario/candidate as FAIL.

Dataset quality:

8. Coverage uses `present_expected_bucket_count`, not raw `actual_candle_count`.
9. Coverage never exceeds 100 even with duplicate/unexpected rows.
10. Missing bucket detection remains deterministic after streaming refactor.
11. Existing OHLC, non-positive price, negative volume, missing candle, and unknown interval tests continue to pass.
12. Content hash remains deterministic across repeated report generation.
13. A long-range split test or synthetic test verifies that report generation does not require materializing all expected timestamps into a tuple or set. This can be tested indirectly by asserting bounded missing samples/ranges and using a range large enough to catch obvious inefficient behavior without making the test slow.

Promotion / legacy:

14. New-generation promotion still refuses failed dataset quality.
15. Legacy lineage promotion, if allowed, records `dataset_quality_legacy_bypass_used=true` and warning `legacy_dataset_quality_bypass_used`.
16. Legacy bypass is not reported as full dataset-quality verification.

Run at least:

```bash
uv run pytest -q
```

If the full suite is too slow, run the smallest relevant subsets first, then the full suite if feasible. In the final report, state exactly what was run.

Operational and recovery constraints:

* Do not weaken live safety gates.
* Do not change live arming behavior.
* Do not write runtime artifacts into the repository.
* Preserve existing `PathManager` and repository-external artifact policy.
* Preserve deterministic hashing.
* Do not silently ignore unsupported fields.
* Do not hide failures behind warning-mode unless the manifest or legacy compatibility path explicitly says so.
* Keep all new failure reasons deterministic, reason-coded, and searchable.
* Do not introduce heavy dependencies.
* Avoid broad top-of-book work in this patch unless it is trivial and fully testable.

Observability requirements:

Execution calibration reports and candidate/promotion evidence should make it obvious when failure came from:

* partial fill rate exceeding scenario assumption
* unfilled/order failure rate exceeding scenario assumption
* insufficient sample count
* failed execution-quality gate
* slippage assumption breach
* latency assumption breach
* market/interval mismatch
* content hash mismatch

Dataset quality reports should make it obvious when failure came from:

* missing candles
* low expected-bucket coverage
* unexpected buckets
* duplicate keys
* non-monotonic timestamps
* interval mismatch
* OHLC invariant violation
* non-positive prices
* negative volume

Promotion diagnostics should distinguish:

* normal dataset-quality verification
* failed dataset quality
* missing dataset quality
* explicit legacy dataset-quality bypass

Documentation requirements:

Update `docs/research-validation.md` after code changes. Keep wording concrete and operator-facing.

Add or clarify:

* Calibration now checks partial fill rate and unfilled rate against scenario assumptions.
* Calibration can fail on insufficient sample count and failed execution-quality gate status.
* `coverage_pct` is based on expected buckets actually present, not raw row count.
* If `dataset_quality_gate_status=FAIL`, do not promote. Repair or rebuild the candle dataset, rerun research-backtest, and verify the corrected `dataset_quality_hash`.
* Legacy dataset-quality bypass is compatibility-only and is not full new-generation evidence.
* Top-of-book storage/research join remains a separate next PR unless implemented here.

Do not claim top-of-book or intra-candle path support unless it is actually implemented and tested. Continue to state that candle-only research remains path-limited.

Final report format:

Return a concise engineering report with these sections:

1. Summary

   * What structural gaps were verified.
   * What was changed.
   * What remains out of scope.

2. Hypothesis Review
   For each hypothesis 1–5:

   * status: verified / refuted / partially verified / not fully investigated
   * code evidence
   * resulting action

3. Implemented Changes

   * Files changed
   * New function arguments, report fields, reason codes, and gates
   * Backward compatibility behavior

4. Validation and Tests

   * Exact commands run
   * Test results
   * Any tests not run and why

5. Operational Impact

   * How operators will see calibration failures
   * How operators will see dataset quality failures
   * How legacy bypass is surfaced
   * Whether promotion/live readiness behavior changed

6. Remaining Risks / Follow-up

   * Especially top-of-book snapshots, quote-aware research execution, full orderbook depth, intra-candle path, and long-range historical backfill

7. Suggested Next PRs

   * P0/P1/P2/P3 priority list for anything not completed

Do not produce a generic architecture essay. Make concrete repository changes where feasible, run tests, and report with exact file/function references.

