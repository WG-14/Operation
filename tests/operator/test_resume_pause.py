from __future__ import annotations

from tests.support.operator_commands import (
    test_pause_disables_trading_via_persistent_runtime_state,
    test_manual_pause_then_resume_success_path,
    test_pause_resume_state_transition_contract,
    test_pause_resume_state_transition_reports_resume_gate_summary,
    test_resume_refuses_when_blockers_remain_after_pause,
    test_resume_live_recent_fill_replay_does_not_fail_with_filled_to_partial,
    test_resume_live_accepts_injected_reconcile_dependencies,
    test_resume_refuses_when_unresolved_state_exists_without_force,
    test_resume_refused_when_ambiguous_submit_only_weakly_matches_recent_fill,
    test_resume_runs_preflight_reconcile_and_refuses_when_recovery_required,
    test_extension_invariant_unresolved_or_recovery_required_state_never_auto_resumes,
    test_resume_refuses_when_reconcile_has_balance_split_mismatch,
    test_live_dry_run_reconcile_uses_accounts_cash_not_start_cash_for_resume,
    test_resume_refuses_when_kill_switch_halt_has_open_position,
    test_resume_blocks_risk_halt_when_only_matched_dust_policy_review_remains,
    test_resume_refuses_when_dust_residual_policy_requires_review,
    test_resume_allows_risk_halt_when_exposure_is_flat,
    test_resume_live_clears_post_trade_reconcile_halt_after_flatten,
    test_resume_non_risk_halt_with_open_exposure_message_is_unchanged,
    test_resume_refuses_when_halt_state_unresolved_even_without_open_orders,
    test_resume_refuses_when_last_reconcile_failed,
    test_resume_force_refuses_when_last_reconcile_failed,
    test_resume_force_refuses_when_startup_safety_gate_blocked,
    test_resume_force_rejects_startup_blocker_before_clearing_manual_pause,
    test_resume_force_refuses_when_halt_state_unresolved,
    test_resume_force_rejects_initial_reconcile_failure_with_operator_readable_reason,
    test_resume_auto_clears_stale_initial_reconcile_halt_after_clean_reconcile,
    test_resume_force_enables_for_safe_manual_pause,
    test_cancel_open_orders_skips_stray_remote_orders_and_reports_resume_gate,
    test_resume_refusal_prints_explicit_blocking_reasons_header,
    test_build_resume_guidance_prefers_manual_recovery_required_over_dust_review,
    test_resume_eligibility_allows_matched_harmless_dust_when_policy_marks_it_tracked_only,
    test_resume_eligibility_keeps_unresolved_open_order_block_even_when_dust_is_resume_safe,
    test_resume_eligibility_clears_stale_lock_halt_after_successful_reconcile_evidence,
    test_reconcile_live_command_reports_resume_gate_contract,
    test_recover_order_does_not_auto_resume_trading,
    test_resume_succeeds_after_manual_recovery_clears_recovery_required,
    test_halt_resume_flow_requires_manual_recover_order_before_resume,
    test_repair_completed_with_no_blockers_auto_clears_pause,
    test_resume_blocked_when_emergency_flatten_unresolved,
)
